<Window x:Class="CVAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Views="clr-namespace:CVAssistant"
        mc:Ignorable="d"
        Background="#FF333333"
        Title="CVAssistant" 
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing"
        >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="35"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="1*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Menu Grid.Row="0" Grid.Column="0">
            <MenuItem Header="Test">
                <MenuItem Header="Test">

                </MenuItem>
            </MenuItem>
            <MenuItem Header="Test">

            </MenuItem>
        </Menu>

        <Views:ConstructorUserControl Grid.Column="0" Grid.Row="1" Grid.RowSpan="2">

        </Views:ConstructorUserControl>

        <GridSplitter Grid.Column="1" 
                      Grid.Row="0" 
                      Grid.RowSpan="3" 
                      Width="5" 
                      HorizontalAlignment="Center" 
                      VerticalAlignment="Stretch">
        </GridSplitter>

        <FlowDocumentReader x:Name="DocViewer" Grid.Row="1" Grid.Column="2">
            <FlowDocument>
                <FlowDocument.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Margin" Value="5"/>
                    </Style>
                </FlowDocument.Resources>
            </FlowDocument>
        </FlowDocumentReader>

        <StackPanel Grid.Row="2" Grid.Column="2" Orientation="Horizontal">
            <Button Click="Button_Click" Margin="3">Gen</Button>
            <Button Click="Button_Click_1" Margin="3">Show</Button>
        </StackPanel>

    </Grid>
</Window>
